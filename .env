VITE_API_URL=
# Dev: leave empty to use the Vite proxy (no CORS issues).
# VITE_API_TARGET overrides the proxy target, e.g. VITE_API_TARGET=http://127.0.0.1:8000
# Prod build: set VITE_API_URL to the backend base URL.
